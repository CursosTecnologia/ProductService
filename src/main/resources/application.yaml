spring:
  application:
    name: Product

  datasource:
    url: jdbc:postgresql://localhost:5432/DBVentaOnline
    username: postgres
    password: Admin7
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
